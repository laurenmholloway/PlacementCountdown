@model IEnumerable<PlacementCountdown.Models.Student>

@{
    ViewBag.Title = "Index";
}

<div id="countdown">

</div>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
    

@foreach (var item in Model)
{  
        <div class="col-sm-6 col-md-3 container">
            <div class="thumbnail">
                @*<img id ="headshots" class="img-responsive img-circle" src="~/headshots/@Html.DisplayFor(modelItem => item.Photo)" alt="@Html.DisplayFor(modelItem => item.FirstName) @Html.DisplayFor(modelItem => item.LastName) Headshot">*@
                <div class="caption">
                    <h3>@Html.DisplayFor(modelItem => item.FirstName) @Html.DisplayFor(modelItem => item.LastName)</h3>
                    <p>Position: @Html.DisplayFor(modelItem => item.JobTitle)</p>
                    <p>Company: @Html.DisplayFor(modelItem => item.CompanyName)</p>
                    <p><a href="#" class="btn btn-primary" role="button">Button</a> <a href="#" class="btn btn-default" role="button">Button</a></p>
                    <p>@Html.ActionLink("Edit", "Edit", new { id = item.StudentID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.StudentID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.StudentID })</p>
                </div>
            </div>
        </div>

            @*@Html.DisplayFor(modelItem => item.PlacementStatus)*@
  
}

